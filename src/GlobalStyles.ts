import { createGlobalStyle } from 'styled-components'

export const GlobalStyles = createGlobalStyle`
  input {
    border-radius: 3px;
    padding: 3px 7px;
    outline: none;
    width: 100%;
  }

  button {
    outline: none;
    background: none;
    border-radius: 5px;
    border: none;
    cursor: pointer;
  }
`
